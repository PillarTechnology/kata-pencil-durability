import unittest

class TestWriterMethods(unittest.TestCase):
    def test_random(self):
        self.assertEquals(1,1)
# writer should be able to 
    # use a pencil
    # write text on paper  -- need paper class?
    # text written by the pencil should always be appended to existing text on the paper

# edit 
    #I want to edit previous text
        # exisiting text cannot shift
        # place a word in white space
        # when letters collide after  they should be represented by the "@" character